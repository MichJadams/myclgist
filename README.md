# whote_is_there

A new Flutter project.

to build release 
`flutter build apk --release`
to install that newly built releast 
`flutter install` or `adb install build/app/outputs/flutter-apk/app-release.apk`
